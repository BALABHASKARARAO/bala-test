# Node.js
# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '14.x'
  displayName: 'Install Node.js'

- script: |
    npm install
    #npm run build
    npm run test -- --coverage . --watchAll=false
    npm install -D jest-sonar-reporter sonarqube-scanner
    npm run test -- --coverage . --watchAll=false
    npx sonar-scanner Dsonar.organization=balabhaskararaog -Dsonar.projectKey=React-SonarQube -Dsonar.login=1ea7803c9fed295232eb82644f9068bc76b36725
  displayName: 'npm install , testbuild'
